import * as _worker from './worker';
import _collectors from './collectors';
import { setup as _setup } from './setup';

export const collectors = _collectors;
export const worker = _worker;
export const setup = _setup;
